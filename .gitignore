# Python / uv
.venv/
__pycache__/
*.pyc
*.pyo
.python-version

# Build artifacts
dist/
build/
*.egg-info/

# Test/sample audio and derived artifacts
samples/
.podedit/
*.transcript.json
# But the hand-curated ASR evaluation references are version-controlled.
!eval/asr/**/reference.transcript.json
*.16k.wav
benchmarks.jsonl

# Test cache
.pytest_cache/

# Local editor / OS
.DS_Store
.vscode/
.idea/

# Per-user Claude Code permission settings (shared .claude/settings.json,
# if any, stays tracked — by convention .local is personal-only).
.claude/settings.local.json
